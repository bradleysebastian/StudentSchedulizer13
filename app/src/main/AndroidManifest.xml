<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beardleysebastian.studentschedulizer13">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MonthlyWorkload"></activity>

        <receiver
            android:name=".NotiReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".UpdateCourseNoteActivity" />
        <activity android:name=".NewCourseNoteActivity" />
        <activity android:name=".CourseNoteDetailActivity" />
        <activity android:name=".AllCourseNotesActivity" />
        <activity android:name=".UpdateAssessNoteActivity" />
        <activity android:name=".NewAssessNoteActivity" />
        <activity android:name=".AssessNoteDetailActivity" />
        <activity android:name=".AllAssessNotesActivity" />
        <activity android:name=".UpdateTermActivity" />
        <activity android:name=".UpdateCourseActivity" />
        <activity android:name=".UpdateAssessActivity" />
        <activity android:name=".NotesAssessActivity" />
        <activity android:name=".NotesCourseActivity" />
        <activity android:name=".NewAssessActivity" />
        <activity android:name=".NewCourseActivity" />
        <activity android:name=".NewTermActivity" />
        <activity android:name=".AssessDetailActivity" />
        <activity android:name=".AllAssessActivity" />
        <activity android:name=".CourseDetailActivity" /> <!-- android:parentActivityName=".AllCoursesActivity" -->
        <!-- /> -->
        <activity android:name=".AllCoursesActivity" /> <!-- android:parentActivityName=".TermDetailActivity" -->
        <!-- /> -->
        <activity android:name=".TermDetailActivity" /> <!-- android:parentActivityName=".AllTermsActivity" -->
        <!-- /> -->
        <activity android:name=".AllTermsFragment" />
        <activity android:name=".AllTermsActivity" /> <!-- android:parentActivityName=".MainActivity" -->
        <!-- /> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
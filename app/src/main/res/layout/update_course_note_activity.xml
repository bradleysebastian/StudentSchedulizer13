<?xml version="1.0" encoding="utf-8"?>
<ScrollView  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">
<LinearLayout
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UpdateCourseNoteActivity">

    <TextView
        android:id="@+id/upd_course_note_title"
        android:textAlignment="center"
        android:textSize="@dimen/list_text_size"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/upd_course_note" />

    <EditText
        android:id="@+id/upd_course_note_inputtxt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="@dimen/list_text_size"
        android:layout_weight="2"
        android:ems="10"
        android:inputType="textMultiLine"
        android:hint="@string/new_course_note_hint" />

    <Button
        android:id="@+id/upd_course_note_save"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/save_button"
        android:onClick="updCourseNote"
        style="@style/ff_button_style"/>

</LinearLayout>
</ScrollView>